services:
  savant-site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: savant-docs
    ports:
      - "3000:3000"
    restart: unless-stopped
    volumes:
      # Persist the tweet cache across builds/restarts.
      - ./.cache:/app/.cache
